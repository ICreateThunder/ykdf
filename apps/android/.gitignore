# Build outputs
build/
.gradle/
local.properties

# Native libraries are produced by build-native.sh (cargo-ndk), not committed.
app/src/main/jniLibs/

# The Gradle wrapper (gradlew + gradle/wrapper/) IS committed for a reproducible
# build; only the local cache directory is ignored.
