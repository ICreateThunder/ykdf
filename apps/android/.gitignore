# Build outputs
build/
.gradle/
local.properties

# Native libraries are produced by build-native.sh (cargo-ndk), not committed.
app/src/main/jniLibs/

# Gradle wrapper jar is generated locally (`gradle wrapper`); not vendored here.
.gradle
